// import Image from 'next/image';
import Navbar from './Navbar';

const Header = () => {
    return (
        <header className="flex items-center justify-between p-[0.63rem] bg-blue3 text-white">
            <Navbar />
        </header>
    )
};

export default Header;